# Electron Lifecycle Prompt

```text
Экономно.

Исправь поведение жизненного цикла Electron:
<описание>

Правила:
- сначала смотри `src/main.js`;
- если меняется IPC API, проверь `src/preload.js`;
- не трогай UI без необходимости;
- не читай `dist` и `node_modules`;
- не запускай build/tests/git;
- кратко опиши результат.
```
