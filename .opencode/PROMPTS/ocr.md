# OCR Prompt

```text
Экономно.

Измени OCR-поведение:
<описание>

Правила:
- для OCR pipeline смотри `src/main.js`;
- для выбора области смотри `src/select.html`, `src/select.js`, `src/select.css`;
- для статусов OCR в главном окне смотри `src/renderer.js`;
- не меняй перевод/словарь без необходимости;
- не запускай build/tests/git.
```
